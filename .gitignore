# LaTeX compilation files
*.aux
*.log
*.out
*.fdb_latexmk
*.fls
*.synctex.gz

# Ignore PDF files everywhere except docs/ (for GitHub Pages)
*.pdf

# Allow PDFs in docs/ folder for GitHub Pages
!docs/*.pdf

# Allow PDFs in resume/ folder (will be generated)
!resume/*.pdf

# macOS
.DS_Store
